//devuelve un array con todos, no solo el primero
const card = document.querySelectorAll('.card');
console.log(card);

//sino existe devuelve una lista vacía
const patata = document.querySelectorAll('.patata');
console.log(patata);

//query específica
const info = document.querySelectorAll('.premium .info');
console.log(info);

//seleccionar elementos html por su etiqueta
const navegacion = document.querySelectorAll('nav');
console.log(navegacion);
